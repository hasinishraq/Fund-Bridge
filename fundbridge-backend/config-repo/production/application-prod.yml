eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_SERVER_URL:https://discovery.fundbridge.internal/eureka/}
    register-with-eureka: true
    fetch-registry: true
management:
  endpoints:
    web:
      exposure:
        include: health,info
server:
  port: 0
